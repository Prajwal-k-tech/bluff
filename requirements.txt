fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
PyJWT>=2.8.0
bcrypt>=4.1.0
python-multipart>=0.0.6
# CPU-only torch for the deployed backend (PureNNBot imports torch at server
# startup; CUDA wheels exceed free-tier disk). Local training uses the same
# CPU build (torch 2.14.0+cpu); GPU runs pass --device explicitly.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
